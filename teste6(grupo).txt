start
    float base, altura, area;

    print({Informe o valor da base do triângulo: });
    scan(base);

    print({Informe o valor da altura do triângulo: });
    scan(altura);

    // Calcula a área do triângulo
    area = (base * altura) / 2;

    // Exibe o resultado
    print({A área do triângulo retângulo é: });
    print(area);
exit
